const text = "Shiroyuki is built using JavaScript on Node and deployed on Google Cloud Platform. It is connected to a MongoDB database via Mongoose to store all the listing data. It mainly uses promises and async/await functions to wait for user input from the Discord API.";

export default text;