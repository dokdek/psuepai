const text = "Users are able to easily create and search for listings by sending the bot a command. The bot will then process the command and either read or write to the connected database and send the user via a DM with only the results that matched their search criteria."

export default text;