const text = "Choco is built using JavaScript on Node and deployed on Google Cloud Platform that implements the Discord.js Node module which allows for 100% promise based interaction with the Discord API. It is connected to a MongoDB database via Mongoose to keep track of user information and deployed on Google Cloud Platform. It utilizes mainly promises and async/await functions to communicate to the API and database. A script was created to query various external APIs to gather information to build the data model to populate the database of kanji and vocabulary. The script iterates through the vocabulary list and queries a Japanese-English dictionary API to extract the readings and meanings.";

export default text;