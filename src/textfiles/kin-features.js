const text = "The site is able to authenticate users via a REST API and jwt tokens. Once logged in, it queries the database for the user's specific information and builds the UI based on that, displaying all their accounts, transactions and budget. Users are able to add, delete and update their budget."

export default text;