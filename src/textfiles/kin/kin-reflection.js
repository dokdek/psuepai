const text = "Kin is a full stack website deployed on Heroku featuring a React front end and a Node/Express server that is connected to MongoDB. The front end is styled using Material UI and uses modern React features such as hooks and functional components rather than classes. The back end is an Express server that is connected to MongoDB via Mongoose and uses Axios for REST API requests from the front end. Finally, user authentication is handled via Firebase and a JSON web token is issued and stored in the user’s cookies for subsequent requests to protected routes.";

export default text;