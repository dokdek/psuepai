const text = "The site is able to authenticate users and connect to a MongoDB database via a REST API built on Node/Express. Once logged in, it queries the database for the logged in user's specific information and builds the UI based on that, displaying all their accounts, transactions and budget. Users are able to create custom categories and payment accounts, as well as add, delete, and update their budget."

export default text;