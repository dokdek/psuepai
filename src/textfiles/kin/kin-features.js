const text = "The site is able to auth and connect to an Express backend via a custom REST API and jwt tokens. Once logged in, it queries the database for the logged in user's specific information and builds the UI based on that, displaying all their accounts, transactions and budget. Users are able to add, delete and update their budget, categories, and payment accounts."

export default text;